# Пусть требуется построить проект из одного исходного файла, в результате чего должен быть создан исполняемый модуль.

project(test_cmake) # Задаёт имя проекта.

add_executable(test_cmake main.cpp) # Определяет новую цель построения с именем test_cmake и исходным файлом main.cpp.
